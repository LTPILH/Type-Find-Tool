t_shm   shmat   :   file exist
	每当子进程结束，我调用shmdt断开共享内存的映射，好像就没事了

对t_file_path_index的写会乱
	把t_man的"rt+" -> "rt", t_fbuild的"at+" -> "at", 好像就没事了

t_shm.c 中 t_readshm(), 不把text[len]='\0'去掉，segptr和sz就会变化，为什么？


用信号量限制一下子进程的数量会不会好一点
